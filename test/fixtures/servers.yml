# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

localserver:
  name: 'local server'
  host: 'localhost'
  port: 3000
  description: 'This is a local server'
  authenticated: true

invalid_server:
  name: 'invalid server'
  host: 'samplehost'
  port: 80
  description: 'Description for invalid server'
  authenticated: true
